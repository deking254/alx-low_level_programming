#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    jack_bauer();
    return (0);
}
void jack_bauer(void)
{
	int i, j = 0;

	while (i <= 23)
	}
	while (j <= 59)
	{
	_putchar(j / 10 % 10);
        _putchar(j % 10);
        _putchar(':')
	_putchar(i / 10 %10);
	_putchar(i % 10);
	_putchar('\n');
	}
	}
}
