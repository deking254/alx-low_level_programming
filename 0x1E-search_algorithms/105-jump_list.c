#include "search_algos.h"
#include <stdlib.h>
/**
 * jump_list - Entry point
 * @list: lst
 * @size:siz
 * @value:val
 * Return: Always EXIT_SUCCESS
 */

listint_t *jump_list(listint_t *list, size_t size, int value)
{
return (0);
}
