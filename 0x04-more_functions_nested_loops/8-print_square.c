#include "main.h"

/**
 * print_square - check the code
 * @size: integer
 * Return: Always void.
 */
void print_square(int size)
{
	int i;

	if (size <= 0)
	{
	_putchar('\n');
	}
	else
	{
	for (j = 0; j < n; j++)
	{
	for (i = 0; i < n; i++)
	_putchar(35);
	_putchar('\n');
	}
	}


}
